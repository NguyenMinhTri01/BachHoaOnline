import admin_M from '../models/admin.model';



// const getInfoAdmin = (token) => {
//   return new Promise((resolve, reject) => {
//     jwt.verify(token, process.env.SECRET_AUTH_ADMIN, async (err, result) => {
//       if (err){
//         return resolve(false);
//       }
//       let info = await admin_M.findAdminById(result.data);
//       return resolve(info);
//     });
//   })
// }


module.exports = { x : null}